
__metaclass__ = type # make sure we use new-style classes

class MockObject(object):
  """ An empty object to which one can add properties dynamically 
      (which doesnt work with object
  """
  pass

